import styled from 'styled-components';

export const Wrap = styled.div`
  font-size: 0;
  line-height: 0;

  border-radius: 4px;

  overflow: hidden;
`;

export const Image = styled.img`
  width: 32px;
  height: 32px;
`;
