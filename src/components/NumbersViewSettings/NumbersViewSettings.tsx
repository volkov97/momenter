import React from 'react';
import { Select } from 'antd';

import { Wrap } from './NumbersViewSettings.styled';

export const NumbersViewSettings: React.FC = () => (
  <Wrap>
    select will be here
    <Select />
  </Wrap>
);
